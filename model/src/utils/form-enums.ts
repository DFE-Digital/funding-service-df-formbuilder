export enum DataImportStatus {
    INITIAL = "initial",
    UPLOADED = "uploaded",
    VALIDATED = "validated",
}

export enum ListFormatOptions {
    BULLETS = "bullets",
    NUMBERED_ITEMS = "numbered_items",
    LINKS = "links",
}
